# Each app manages its own .gitignore; these are repo-wide safety nets
# in case a subfolder's rules are ever loosened or a new app is added.
**/node_modules/
**/.next/
**/.vercel
**/.env
**/.env.local
**/.env.*.local
**/*.pem
**/*.ppk
